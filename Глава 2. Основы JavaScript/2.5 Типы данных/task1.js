// Шаблонные строки. Что выведет этот скрипт?

let name = "Ilya";

alert(`hello ${1}`); // hello 1, где 1 - число.

alert(`hello ${"name"}`); // hello name, где name - строка.

alert(`hello ${name}`); // hello Ilya, поскольку тут используем переменную.
