// Повторять цикл, пока ввод неверен.
// Создаём переменную.
let num;
// Пишем что необходимо сделать.
do {
  num = prompt("Введите число больше 100?", 0);
} while (num <= 100 && num); //Условие для повторения.
