// Покажите знак числа.
// Показываем сообщение и запрашиваем ввод текста от пользователя.
let value = prompt("Введите число", 0);
// Пишем условия.
if (value > 0) {
  alert(1);
} else if (value < 0) {
  alert(-1);
} else {
  alert(0);
}
